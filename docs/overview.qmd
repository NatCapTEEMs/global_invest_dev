---
title: "Global InVEST"
subtitle: "The GEP service library: state and open questions."
# The deck's numbers, one home: edit here and every slide referencing {{< meta ... >}} updates.
terrestrial_total: "$5.365T"
coastal_eez: "$22.84bn"
coastal_all: "$102.18bn"
coastal_report: "$26.65bn"
crop_total: "$678bn"
cp_total: "$36.72bn"
extractive_total: "$99.54bn"
extractive_alt: "$203bn"
extractive_factor: "0.49"
renewable_total: "$170.83bn"
renewable_ref: "$134.6bn"
poll_raster: "$18.28bn"
poll_fao: "$515bn"
erosion_total: "$15.54bn"
livestock_ind: "$136bn"
livestock_world: "$2.29T"
n_services: "23"
n_replicating: "9"
n_awaiting_file: "3"
fisheries_total: "$29bn"
fisheries_subsistence_total: "$7.92bn"
water_supply_total: "$144.68bn"
water_use_ag_total: "$1.07T"
water_use_all_total: "$36.61T"
air_filtration_total: "$17.81bn"
sandstorm_total: "$595.36bn"
wq_usd: "$638.34bn"
wq_intl: "$655.91bn"
flood_total: "$112.78bn"
lcr_total: "$14.19bn"
xe_total: "$1,048.9bn"
xe_committed: "$1,140.4bn"
timber_total: "$88.74bn"
landslide_total: "$770.7M"
landslide_deaths: "570.66"
recreation_total: "$1.73bn"
fire_cause_total: "-$1.3bn"
fire_baseline_total: "-$22.4bn"
fire_area_total: "+$16.7bn"
renewable_wind: "$110.31bn"
renewable_solar: "$53.95bn"
renewable_geothermal: "$6.56bn"
stormwater_volume: "185.85bn m3"
stormwater_value: "$185.85bn"
hydro_manuscript: "$287.7bn"

format:
    revealjs:
        embed-resources: true
        theme: simple
        margin: 0.08
        scrollable: true
        code-fold: show
        slide-number: true
        preview-links: auto
        css: styles.css
        incremental: true
---

# Motivation

## Nature is invisible in the accounts

- Ecosystems supply services the economy depends on, from pollination and soil retention to coastal protection and carbon storage, yet none of them appear in standard national accounts.
- Unmeasured, their loss shows up later as lower yields and higher damages.
- The Gross Ecosystem Product (GEP) account values what ecosystems supply, country by country and service by service, so natural capital can be tracked alongside GDP.
- The same service models also drive scenario analysis. They trace how climate and land-use scenarios change the services economies depend on, and the results feed the macroeconomic pipelines.

## Why one reviewed library

- Different contributors developed the GEP service models in different repos, each with its own layout, path conventions and aggregation choices.
- The library brings them into one repo with one layout. Every service runs the same way and writes the same per-country table the GEP account reads.
- The library gives policymakers a tool to price nature risk as the scenarios they use today carry no ecosystem-service feedback.

# What global_invest is

## One module per service

- The library holds one folder per ecosystem service, all on the same file convention.
- {{< meta n_services >}} services carry their GEP method in the library, with their input data in the shared base data.
- Some services also produce a scenario shock for the macroeconomic pipeline. A shock exists where the scenarios' physical changes (the land-use maps or the climate pathway) reach the service's science.
- The library is scenario agnostic. A caller supplies its own scenarios (the maps, the years and the naming) through configuration.

## How every GEP number is built

- Every service's number is the same construction. It multiplies a quantity of service delivered by a price and by nature's share of it, summed to one row per country.
- The three parts differ per service (recreation's quantity is visits and its price is travel cost, livestock's nature share is the ecosystem-provided part of feed), but the construction never changes, which is why one configuration table can describe every service.

## The code structure {.smaller}

| File | In paper terms |
|---|---|
| functions.py | The equations, pure science. |
| tasks.py | The estimation steps. Each writes one output. |
| initialize.py | The table of contents (the list of steps, and the entry point other pipelines call). |
| run_\<service\>.py | The main script. Opens a workspace and runs the steps. |
| method.qmd | The written explanation of the method. |

- run_\<service\>.py asks initialize.py for the list of steps, each step in tasks.py calls the equations in functions.py and writes its output, and the next step reads that output.
- A service with a shock has a second runner for it, run_\<service\>_shock.py (one runner per task tree).
- Three shared CSVs carry the configuration (the next slide).

## The template every service follows

- Every run file lists the steps of its pipeline in one build_task_tree function.
- A service reads its settings from rows in three shared CSVs.
- es_config.csv: one row per service, with the GEP formula (quantity, price, attribution) plus the land base, the base year, the price convention and the crosswalk to the status sheet.
- es_scenarios_test.csv: the standard SEALS scenarios, with matching test maps shipped, so a standalone shock run works on any machine.
- es_parameters.csv: the run parameters and the references to each service's data.
- Results aggregate to one row per country (r250). Sub-national tables (r264, marine r566) serve as computation surfaces and maps.

## The library's two callers

- Each service module serves two callers. The GEP account reads the valuation (one row per country), and the macroeconomic pipeline reads the scenario shock (a table of productivity deviations).
- Both read the same functions file, so a change to the science reaches the account and the scenarios at once.

# Where things stand

## The state in numbers

- {{< meta n_services >}} services are in the library, and {{< meta n_replicating >}} of them reproduce a committed reference or the manuscript.
- Eighteen compute their reported number here from source data. Four report a value another team computed, because no chain behind it exists in the library: coastal protection, urban cooling, stormwater's retained volume, and water quality's headline column. Non-timber forest products has no number yet.
- {{< meta n_awaiting_file >}} await one file to finish verification: recreation, commercial fisheries and landslide each need a reference output we could not find. Erosion's replication rerun is under way.
- Livestock's numbers are provisional until the feed-share attribution replaces the land rental rate standing in for it.

## What each service is {.smaller}

| Service | What it values |
|---|---|
| terrestrial_carbon | carbon stored by land ecosystems, priced at the social cost of carbon |
| coastal_carbon | carbon in mangroves, salt marshes and seagrass |
| pollination | crop production attributable to wild pollinators |
| erosion | soil the land cover keeps in place, valued through crop productivity |
| landslide_mitigation | landslide deaths avoided by forest cover, valued at a value of a statistical life |
| fisheries | commercial capture-fisheries rent. Subsistence catch value |
| crop_provision | the value of crop production |

: {tbl-colwidths="[30,70]"}

## What each service is (continued) {.smaller}

| Service | What it values |
|---|---|
| livestock_provision | livestock production attributable to ecosystem-provided feed |
| coastal_protection | storm damage avoided by mangroves |
| extractive_materials | mineral resource rents |
| renewable_energy | wind, solar and geothermal production value |
| recreation | visits to natural recreation sites, valued at travel cost |
| fire_protection | wildfire damage avoided by nature's fire regulation |
| water_supply | hydropower rent, and water withdrawal priced by sector |

: {tbl-colwidths="[30,70]"}

## What each service is (continued) {.smaller}

| Service | What it values |
|---|---|
| air_filtration | deaths avoided by vegetation removing air pollution |
| sandstorm_prevention | deaths avoided by ecosystems suppressing windblown dust |
| water_quality | water-treatment costs avoided by nutrient retention |
| flood_control | flood damage avoided by ecosystems |
| local_climate_regulation | cooling-energy costs avoided by urban vegetation |
| extractive_energy | fossil-fuel resource rents (gas, coal, petroleum) |
| timber_provision | the net value of timber harvests, after harvest and transport costs |

: {tbl-colwidths="[30,70]"}

## Services and totals (base year 2019) {.smaller}

Full per-service status, one row per sheet service, updated as work lands: [the library status sheet](https://docs.google.com/spreadsheets/d/1sQOUT7TRKVLxc-TSfPWeuCFm0vwk-6iLk-AnA6jyEiY).

| Service | Total | Verified |
|---|---|---|
| terrestrial_carbon | {{< meta terrestrial_total >}} | yes. Reproduces the manuscript's total |
| coastal_carbon | {{< meta coastal_eez >}} EEZ-only, {{< meta coastal_all >}} all rows | yes. Reproduces the author's reported numbers |
| crop_provision | {{< meta crop_total >}} | yes. Reproduces the reference table |
| livestock_provision | provisional | no. Awaits the GLEAM intake data and a reference output |
| coastal_protection | {{< meta cp_total >}} | no. Both source tables already carry a final per-country value, so there is nothing here to reproduce |
| extractive_materials | {{< meta extractive_total >}} | yes. Reproduces the author's reference table |
| renewable_energy | {{< meta renewable_total >}} | partially. The code and the drive's reference CSVs give different values, and which one is current is the open question |

: {tbl-colwidths="[22,32,46]"}

## Services and totals (continued) {.smaller}

| Service | Total | Verified |
|---|---|---|
| pollination | {{< meta poll_raster >}} raster side (2023). {{< meta poll_fao >}} FAO side | partially. Two definitions of the same service, and the decision between them is open |
| erosion | {{< meta erosion_total >}} provisional | not yet. The rerun with the author's original inputs is under way |
| landslide_mitigation | {{< meta landslide_total >}} ({{< meta landslide_deaths >}} avoided deaths) | no. No reference output exists to compare against yet |
| fisheries | shock + commercial GEP {{< meta fisheries_total >}} provisional + subsistence {{< meta fisheries_subsistence_total >}} | partially. Subsistence reproduces its committed output, and the commercial run has no reference output yet |
| recreation | {{< meta recreation_total >}} provisional | no. The author's output table is not reachable |
| fire_protection | {{< meta fire_cause_total >}} provisional, one of three coefficients | partially. Reproduces the committed output, but reads the committed regression coefficients rather than refitting them |
| water_supply | hydropower component {{< meta water_supply_total >}} | yes. Reproduces the drive's committed output for the 95 valued countries |

: {tbl-colwidths="[22,32,46]"}

## Services and totals (continued) {.smaller}

| Service | Total | Verified |
|---|---|---|
| air_filtration | {{< meta air_filtration_total >}} | yes. Reproduces the drive's committed workbook and the manuscript's total |
| sandstorm_prevention | {{< meta sandstorm_total >}} | yes. Reproduces the same workbook's dust channel |
| water_quality | {{< meta wq_usd >}} USD and {{< meta wq_intl >}} intl$ | partially. We recompute the nutrient valuations and reproduce the committed intermediates; the headline international-dollar column is read through |
| flood_control | {{< meta flood_total >}} | partially. Our recompute matches the pipeline's table, Morocco excepted |
| local_climate_regulation | {{< meta lcr_total >}} | no. The committed final is the reported value; its inputs are not available to recompute |
| extractive_energy | {{< meta xe_total >}} (gas + coal + petroleum) | yes. Computed from the public CWoN rent tables, and the committed tables double-count eight rows |
| timber_provision | {{< meta timber_total >}} | yes. All 166 countries match the committed output |
| ntfp | in progress | no. The accessibility run is going on the public roads data |
| stormwater | {{< meta stormwater_volume >}} retained ({{< meta stormwater_value >}} at the placeholder price) | no. The retention run is ours, and no author run exists to compare against |

: {tbl-colwidths="[22,32,46]"}

# Open questions by service

## terrestrial_carbon

- [Values carbon stored by land ecosystems: a zone-by-class density table on the land-cover map, priced at the rental social cost of carbon. Our run reproduces the manuscript's total, once a double-count that entered split countries once per sub-region (China six times, overstating the total by 23.5 percent) was fixed.]{.state}
- **Q1:** does the rental social cost of carbon we apply use the discount rate the account settles on? The price convention embeds one, and the group's guidance asks for 2 percent with 1 and 3 as sensitivities.

## coastal_carbon

- [Values carbon stored in mangroves, salt marshes and seagrass: mapped habitat times per-habitat density, priced like terrestrial carbon. Computed in the library from the habitat maps, and it reproduces the author's reported numbers.]{.state}
- **Q1:** was EEZ-only chosen to avoid double-counting the landward pixels with terrestrial carbon? Most of the mapped habitat sits landward of the shoreline (97.5 percent of mangrove area, 84 percent of salt marsh), so including it multiplies the total by 4.5.
- **Q2:** which exact seagrass and salt marsh input versions produced the report's {{< meta coastal_report >}}? Our replication gives {{< meta coastal_eez >}} with the mangrove component matching, and the report's own seagrass area (62.21 Mha) exceeds the published range it cites (16 to 26.7 Mha).
- **Q3:** if both sides count, a landward mask must prevent double-counting with terrestrial carbon. Does one exist?

## marine_carbon

- [The service sheet lists Terrestrial, Coastal, and Marine carbon. No marine module found in any repo.]{.state}
- **Q1:** is marine carbon in scope for the account?
- **Q2:** if yes, which data drives it?
- **Q3:** most open-ocean carbon sits in the high seas, inside no country's EEZ. Which attribution convention should a per-country account use?
- The sheet lists one more subgroup under the same service, Other Non-Carbon GHG. We could not find a module for it either, and the same three questions apply.

## pollination

- [Values crop production attributable to wild pollinators, from a sufficiency-weighted value raster built from pollinator habitat around cropland. We aggregate that raster to countries ourselves. It is a 2023 raster, and we regenerate it at 2019 once the definition is decided.]{.state}
- **Q1:** is pollination's value the service nature currently delivers ({{< meta poll_raster >}}, the sufficiency-weighted raster) or the production value at stake ({{< meta poll_fao >}}, dependence-weighted FAO value)? The country ranking is identical, only the definition differs. Erosion records the habitat-supplied share, which would make the raster side the consistent choice.
- **Q2:** does the account add pollination on top of crop_provision? Crop multiplies production value by the land rental rate, so both attribute part of the same output to nature.

## erosion

- [Values soil the land cover keeps in place: prevented erosion mapped by running the sediment-delivery model ourselves, valued through crop productivity. A replication run on the author's original input tables is under way, so the number is provisional.]{.state}
- **Q1:** which of the three shock methods is the account's? We compute damage (thresholded area share), service (threshold-free and magnitude-weighted per crop) and service-threshold (service restricted to a fixed severe-pixel set) side by side.

## landslide_mitigation

- [Values landslide deaths avoided by forest cover: slope stability modelled with and without forest root cohesion, the difference turned into avoided deaths and priced at a value of a statistical life. Computed end to end in the library, and the first full run is complete on the cluster.]{.state}
- **Q1:** do you have a per-country output from a v0.2.0 run?
- **Q2:** which input versions did the run behind the v0.2.0 release use?

## fisheries

- [Values commercial capture fisheries as the economic rent of the catch, and subsistence fisheries as the consumptive-use value of home catch. The commercial rent is computed in the library from the CWoN tables. The subsistence component reports the published per-country values, and reproduces them exactly.]{.state}
- **Q1:** should the RCP8.5-for-RCP7.0 substitution stand, or is there a replacement?
- **Q2:** which CWoN vintage produced the shock file (cwon_shocks.har)?
- **Q3:** should the account's fisheries value come from the CWoN resource rents or from the FishStatJ catch pipeline? Do you have a per-country CSV from your own run of the CWoN script?
- **Q4:** does the subsistence value add to the commercial number, or does that number already contain the subsistence catch? Where FAO catch statistics already include subsistence fishing, summing the two counts it twice.

## recreation

- [Values visits to natural recreation sites at travel cost: 1 km sites ranked by land-cover shares, protected areas and road access, then a gravity model sends residents on day trips and tourists on overnight stays into the high-quality sites. Computed end to end in the library.]{.state}
- **Q1:** do you have the source pipeline's results_by_country.csv, for replication?
- **Q2:** did the travel-cost step intend kilometres? It multiplies a USD-per-km fuel cost by a distance measured in the raster's pixel units (degrees), which understates values.

## crop_provision

- [Values crop production from FAOSTAT production values, quantity times producer price per country, times the land rental rate. Computed in the library, and a fresh run reproduces the reference table to the cent.]{.state}
- **Q1:** does the account add crop_provision and pollination? Both attribute part of the same output to nature.
- **Q2:** is subsistence in scope as a separate crop subgroup? Adding it would double-count, because FAOSTAT's production values already include part of what households grow and eat themselves.

## livestock_provision

- [Values livestock production and attributes to nature the share of feed ecosystems provide. Computed in the library the way crop_provision is, but on a land rental rate standing in for the feed share, so the numbers are provisional. On the 65 FAO item codes India recovers from zero to {{< meta livestock_ind >}} and the world total reads {{< meta livestock_world >}}.]{.state}
- **Q1:** should milk and the other missing products count as livestock production? The library's old list covered meat and eggs only and captured 4 percent of India's FAO livestock value, while the method repo's list of 65 products includes milk.
- **Q2:** is nature's share the part of feed that comes from ecosystems (grass, leaves, crop residues, by-products) rather than from crops and concentrates? We have written and tested that formula, and it would replace the land rental rate standing in for it now.
- **Q3:** can you share the GLEAM 3 intake extract (gleam3_dmi.xlsx), and a per-country output to replicate? The public dashboard serves six feed categories where the formula needs eight, so the extract is the blocker, not access.

## coastal_protection

- [Values storm damage mangroves and corals prevent along coastlines. Both source tables already carry a final per-country value, so the module carries the coral value to the base year with a deflator and sums the two. It does not rebuild the underlying damage estimate.]{.state}
- **Q1:** is there a chain behind the two committed value tables that we should be running instead? Today we report the tables' own values, so a discrepancy with the author could not surface.
- **Q2:** should the 9 countries missing from the deflator table be left unvalued rather than zero? They are currently zeroed by the join, which reads as no damage prevented.

## extractive_materials

- [Values mineral extraction as the resource-rent share of mining output. Computed in the library from the World Bank rent and GDP tables, and our run reproduces the reference per country.]{.state}
- **Q1:** what is the source of the 0.49 factor? The valuation multiplies mineral rents times GDP by it, and without it the total would be {{< meta extractive_alt >}} instead of {{< meta extractive_total >}}.
- **Q2:** should the 9 countries with no World Bank rent data stay unvalued rather than zero? We leave them empty today, which is the convention the group's guidance asks us to confirm.

## renewable_energy

- [Values wind, solar and geothermal electricity as generation times price times a resource-rent share. Our run gives {{< meta renewable_wind >}} wind, {{< meta renewable_solar >}} solar and {{< meta renewable_geothermal >}} geothermal, and a fresh run reproduces each to the cent.]{.state}
- **Q1:** which is current, the code's {{< meta renewable_total >}} or the drive CSVs' {{< meta renewable_ref >}}?
- **Q2:** should wind, solar and geothermal be three services in the account or one? The sheet lists three, the library computes them in one chain and already writes a table per resource, so either shape is a configuration change rather than a rewrite.

## fire_protection

- [Values wildfire damage avoided by nature's fire regulation: a per-country fire-persistence coefficient times the country's 2018 burned area gives avoided acres in 2019, valued at EM-DAT damage per burned acre. Our run reproduces the source repo's committed output for all 161 countries.]{.state}
- **Q1:** which of the three coefficients is the account's value? They disagree in sign: baseline persistence gives {{< meta fire_baseline_total >}}, the nature-versus-human cause difference {{< meta fire_cause_total >}}, and the nature-versus-human area difference {{< meta fire_area_total >}}.
- **Q2:** should the 89 countries the regressions do not cover stay unvalued? No data is not zero avoided damage.

## water_supply

- [Values hydropower rent and priced water withdrawal. Hydropower reproduces the drive's committed output for all 95 valued countries. Water use is computed in the library, agriculture {{< meta water_use_ag_total >}} across 126 countries and all-sector {{< meta water_use_all_total >}} across 154.]{.state}
- **Q1:** which hydropower total is the account's, the manuscript's {{< meta hydro_manuscript >}} or the committed output's {{< meta water_supply_total >}}? The first follows a direct-use method, price times generation, the second a resource-rent method.
- **Q2:** why does the reference leave 17 countries empty although the CWoN table values them? We exclude the same 17 by name to reproduce it, which fits our number to the reference rather than deriving it.
- **Q3:** what attribution step turns water use into a nature share? The all-sector total is the gross economic value of water use, close to global GDP.
- **Q4:** how do hydropower and the two water-use components combine into one water_supply value?

## air_filtration

- [Values deaths avoided by vegetation capturing air pollution, priced at a GDP-adjusted value of a statistical life (the monetary value assigned to one avoided death). We recompute the valuation and reproduce the committed totals. The avoided-death counts come from the group's emissions-to-health modelling chain, which we cannot rebuild and take as given.]{.state}
- **Q1:** which VSL build produced the committed value column? The method is documented (US-anchored, adjusted for GDP and life-years) but the raw data shipped beside it is a different vintage and does not reproduce the column.
- **Q2:** which vintage are the avoided-death counts?

## sandstorm_prevention

- [Values deaths avoided by ecosystems suppressing windblown dust, priced the same way as air filtration and computed by the same module. We recompute the valuation and reproduce the committed totals.]{.state}
- The two questions on the previous slide apply here unchanged, because both channels share one chain and one VSL column.

## water_quality

- [Values water-treatment costs avoided by nutrient retention: nitrogen and phosphorus retention per country, scaled by the AQUASTAT domestic-water-use fraction and priced at treatment cost per kilogram. Our rebuild reproduces the committed intermediates for 178 countries. The retention estimates come from an upstream nutrient-delivery model we take as given.]{.state}
- **Q1:** where is the script that converts USD to international dollars? The factors implied by the two committed values sit far from the World Bank's published ones, so we could not identify the conversion, and the committed output is the only place it appears.
- **Q2:** which currency does the account use? This service is in 2019 international dollars, every other service in 2019 market USD.

## flood_control

- [Values flood damage avoided by ecosystems: flood depth by return period, depth-damage functions and the asset map give each country's expected annual damage. Our recompute reproduces the pipeline's table for 146 of 148 non-zero countries.]{.state}
- **Q1:** should the service-flow fraction multiply into the valuation? The pipeline computes one but never applies it, so today's number is expected flood damage rather than damage ecosystems avoid.
- **Q2:** are the FINAL_GEP_FLOOD outputs the current ones?

## local_climate_regulation

- [Values cooling energy avoided by urban vegetation: an urban cooling model gives each city-month a cooling effect, priced at national electricity prices. Our run reproduces the committed final.]{.state}
- **Q1:** is the account's value the plain city sum or the cooling-degree-day correction? The committed final equals the plain sum, so the correction is computed and then not applied.

## extractive_energy

- [Values fossil-fuel resource rents (gas, coal and petroleum) at 2019, on the same commodity convention extractive_materials uses. Computed in the library from the public CWoN rent tables the method appendices name.]{.state}
- **Q1:** are the eight duplicated rows in the committed tables a join error, and which total does the account keep? Christmas Island and Cocos carry Australia's rents, Bouvet and Svalbard Norway's, Tokelau New Zealand's, Western Sahara Morocco's, Palestine Israel's and Kosovo Serbia's, so the committed {{< meta xe_committed >}} counts those countries twice. One country one row gives {{< meta xe_total >}}.

## timber_provision

- [Values timber harvests net of harvest and transport costs. Our reimplementation matches the committed output for all 166 countries.]{.state}
- **No open questions.**

## ntfp

- [Values non-timber forest products as accessible forest area (near roads and rivers) times the CWoN value per hectare. The valuation is written and tested, and the accessibility stage is running on the public roads database, so there is no number yet.]{.state}
- **No open questions.** What is missing is our own run finishing.

## stormwater

- [Values stormwater retained by urban vegetation and soils. An urban stormwater retention run retains {{< meta stormwater_volume >}} per year across 104 countries, wall to wall rather than urban-masked, and the module prices that volume.]{.state}
- **Q1:** what is the intended price per cubic metre retained? The configuration carries a placeholder of 1, so today's value is the volume.
- **Q2:** should the run be masked to urban areas? It currently covers the whole land surface.

# Questions that cross services

## Conventions to settle

- **Base year:** every service with a number values at 2019, except pollination's 2023 raster, which we regenerate once its definition is settled. Confirm 2019.
- **Subsistence:** crop, livestock and fisheries carry subsistence subgroups in the sheet. In scope, per service?
- **Maps:** country values on split-country polygons render partially, so China shows one sixth of its value. May we dissolve to one polygon per country for cartography? It is a display change that cannot affect a total.
- **Case studies:** the sheet marks five services computable only as worked examples rather than all-country accounts (water quality, pest control, health, peak flow, moisture recycling), though water quality now runs globally in the library. Is case-study coverage acceptable for the account?
- **The crosswalk:** the sheet lists roughly 26 services and the library covers 23. We propose a tested crosswalk table, sheet label to module to output, rather than restructuring modules to mirror the sheet.

# What we need

## What we need, by service

- **coastal_carbon:** the shoreline rationale, and the seagrass and marsh input versions behind {{< meta coastal_report >}}.
- **coastal_protection:** whether a chain sits behind the two committed value tables that we should run instead.
- **landslide_mitigation:** one machine-readable v0.2.0 output, its input versions, and which value of a statistical life it uses.
- **fisheries:** confirm the RCP substitution, name the shock file's CWoN vintage, and confirm or replace the rent-based value now running in the library.
- **livestock_provision:** the GLEAM 3 intake extract, the item-grouping confirmation, and a reference output.
- **extractive_materials:** the source of the 0.49 factor.
- **renewable_energy:** whether the code or the drive CSVs are current.
- **extractive_energy:** whether the eight duplicated rows are a join error, and which total the account keeps.
- **recreation:** one reference results_by_country.csv.
- **water_quality:** the conversion to international dollars, and the currency decision.
- **flood_control:** whether the service-flow fraction enters the valuation, and whether the FINAL_GEP_FLOOD outputs are current.
- **water_supply:** which hydropower total is the account's, why the reference leaves 17 countries empty, and the attribution step the water-use components need.
- **fire_protection:** the choice among the three coefficients.
- **air_filtration:** the VSL build behind the committed column, and the vintage of the avoided-death counts.
- **local_climate_regulation:** whether the account takes the plain city sum or the cooling-degree-day correction.
- **stormwater:** the price per cubic metre, and whether to mask to urban areas.
- **Decisions for the meeting:** pollination's definition, the shoreline convention, the map dissolve, base year 2019, subsistence scope, and marine carbon's fate.

# Appendix

## How a run finds its data

- Every input is a reference path relative to the shared base data, and each machine resolves it for itself: the task's own directory first (so finished work is found and skipped), then the project's input folder, then the machine's base_data, then the cloud fallback.
- A run therefore carries no absolute paths. The same run file works on a laptop, on the cluster, or on any machine that stages the same base data.
- Tasks cache their output in the project directory and skip themselves when it already exists, so a rerun recomputes nothing and an interrupted run resumes where it stopped.

## How a pipeline grafts a service

- A caller grafts a service with one call (add_\<service\>_tasks(p) after its land-use step), and the service's tasks slot into the caller's own task tree.
- A CSV cell is a default. The caller's configuration prevails over the shipped CSVs, so the library never needs to know its callers' naming.

## The configuration table (es_config.csv) {.smaller}

| service | base year | price convention | Q | P | λ | land |
|---|---|---|---|---|---|---|
| terrestrial_carbon | 2019 | rental scc r2% | carbon zones raster | prices xlsx | | esa 2019 |
| coastal_carbon | 2019 | rental scc r2% | | prices xlsx | | |
| pollination | 2023 | | value raster | | | |
| renewable_energy | 2019 | | IRENA production | WB prices | CWON rents | |
| extractive_materials | 2019 | | WB GDP | | WB rents share | |
| coastal_protection | 2019 | | CWoN mangroves | | | |

: {tbl-colwidths="[24,12,20,16,12,10,6]"}

- The remaining services' rows follow the same columns. Blank price-convention cells fill as each module's convention is named. Every row is under test the moment it is edited.

## What the tests pin, per service {.smaller}

| Service | What its tests pin |
|---|---|
| terrestrial_carbon | raster math on tiny synthetic inputs. The static shock on a synthetic dependency table |
| coastal_carbon | the two aggregation contracts: only marine EEZ rows are valued, and the r250 collapse |
| pollination | end-to-end on a tiny value raster through real zonal statistics. A split country sums once |
| erosion | the valuation arithmetic itself: elasticity shock, clipping, floor, GPV shares |
| recreation | every method constant against examples small enough to compute by hand: matrices, ring kernels, gravity model, allocation |
| fire_protection | exact replication of the committed reference (161 countries) on every run, plus the reordered-input tripwire |
| water_supply | the annuity identity and exact replication of the committed output |
| air_filtration | benefits and both totals against the workbook. The VSL fill rule. The positional-join guard |
| water_quality | the retention identities against the committed intermediates. The conversion documented as unidentified |
| flood_control | the anchor against the pipeline's own global summary: total, assessed count, zero count |
| local_climate_regulation | the city kwh-x-price identity, the version-lineage sum, the final's parse and totals, the CDD math |
| extractive_energy | components and sum against the three fuel tables. Coal-only countries valued, no-table countries NaN |
| timber_provision | the join and total against the committed output |
| fisheries | header resolution and the HAR read-list union. The CWoN rent-trend math, and the shock's ramp, imputation override and header fallback |
| landslide_mitigation | the chain on synthetic inputs: stability index, discharge, hazard probability, severity expectation, the VSL join and its income-group fallback |
| crop_provision | the FAOSTAT clean-up, the decade rental rates, the thousand-USD conversion, the M49 successor codes, the split-country join |
| livestock_provision | the same steps on its own tables, plus the ecosystem share of feed intake |
| extractive_materials | the rent-share formula, the wide-table melt against the expression it replaced, the byte-order-mark guard |
| renewable_energy | each pipeline step on hand-built tables: the IRENA aggregation, the price conversion, the joins, the base-year selection, the per-resource split |
| coastal_protection | the deflator compounding, mangrove and coral valuation with a split country counted once, the undeflatable-country rule |
| library-wide | every shipped CSV row hydrates, every shock seam stays grafted, the split-country guard stays present |

: {tbl-colwidths="[26,74]"}

- Any new configuration row or service is covered automatically: the shared tests sweep every row of the shipped CSVs and every service's entry points.
- Every service now pins its own arithmetic. Where a chain was extracted from working code, the old and new versions were run on the same inputs and had to agree exactly, and five services reproduce their committed totals to the cent in fresh runs.
