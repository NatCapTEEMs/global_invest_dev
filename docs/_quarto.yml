project:
  type: book
  output-dir: ../HTML_global_invest_docs

book:
  title: "Global InVEST"
  subtitle: "Ecosystem-service valuation (GEP) and scenario shocks"
  author: "NatCapTEEMs"
  date: today
  page-navigation: true
  search: true
  chapters:
    - index.qmd

# overview.qmd is deliberately absent from the chapter list: it renders as a
# standalone revealjs deck, not a book chapter. It is HAND-EDITED source; the
# *_marked.qmd suffix is reserved for generated decks per the devstack spec.

format:
  html:
    theme: yeti
    toc: true
